#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    // Challenge 6 : Compte des Occurrences d'un Caractère

    char A[20];
    printf("entre un mot: ");
    scanf("%s",&A);
    for(int i=0;i<strlen(A);i++){

    }


    return 0;
}
