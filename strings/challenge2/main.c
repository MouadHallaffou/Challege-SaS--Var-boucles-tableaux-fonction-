#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    //Challenge 2 : Longueur de la Chaîne

    char prenom[10];
    printf("entre votre prenom: ");
    scanf("%s",&prenom);
    printf("nrb caractere est : %d",strlen(prenom));

    return 0;
}
